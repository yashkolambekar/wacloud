
Language is a simple `enum` that has language codes for all the languages that whatsapp supports. To use the language enum we just have to import it and reference the key

```ts
import Language from "wacloud";

const english = Language.English;

```